<UserControl x:Class="Mecalux_WPFChallenge.Views.OrdenarTextoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mecalux_WPFChallenge.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Ordenar Texto: " FontSize="18" Margin="0,0,0,20"/>

        <Label Content="Tipo ordenado:" />
        <ComboBox Margin="0,0,0,12"
          ItemsSource="{Binding TiposOrdenamiento}"
          SelectedItem="{Binding TipoOrdenamiento}"
          DisplayMemberPath="Descripcion" />

        <Label Content="Texto:"/>
        <TextBox Text="{Binding Texto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

        <Button Content="Reordenar" Height="30" Background="#2196F3" Foreground="White"/>

        <TextBlock Text="{Binding TextoOrdenado}" FontSize="18" Margin="0,0,0,20"/>
    </StackPanel>
</UserControl>
