<UserControl x:Class="Mecalux_WPFChallenge.Views.AnalizarTextoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mecalux_WPFChallenge.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Ordenar Texto: " FontSize="18" Margin="0,0,0,20"/>

        <Label Content="Texto:"/>
        <TextBox Text="{Binding Texto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

        <Button Content="Analizar" Command="{Binding AnalizarTextoCommand}"  Height="30" Background="#2196F3" Foreground="White"/>

        <Label Content="Palabras:"/>
        <TextBlock Text="{Binding Palabras, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Margin="0,0,0,20"/>
        <Label Content="Espacios en blanco:"/>
        <TextBlock Text="{Binding EspaciosEnBlanco, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Margin="0,0,0,20"/>
        <Label Content="Guiones:"/>
        <TextBlock Text="{Binding Guiones, UpdateSourceTrigger=PropertyChanged}" FontSize="18" Margin="0,0,0,20"/>
    </StackPanel>
</UserControl>
